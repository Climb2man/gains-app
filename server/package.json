{
  "name": "gains-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Personal MCP server for Gains: talk to your own health record (food, calories, weight, steps, Whoop) from any MCP client. Phone pushes an encrypted slice; the server holds it in RAM only and serves read-only tools.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "dev": "MCP_TRANSPORT=http tsx watch src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.0.0",
    "tsx": "^4.22.3",
    "typescript": "^5.7.0"
  }
}
